import styled from "styled-components";

export const Wrapper = styled.section`
    display : flex;
    flex-direction:column;
    gap:10px;

    img{
        align-self: center;
    }
    .heading{
        font-size: 20px;
        font-weight:600;
    }

`